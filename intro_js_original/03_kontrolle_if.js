// Kurz-Einführung in JavaScript

// Kontrollstrukturen

if ( expression ) statement;
if ( 28 > PI * 9 ) {
  var gross = "das stimmt";
}
if ( deinName === "Peter" ) {
  alert( "Es ist Peter" );
} else if ( deinName === "Hannah" ) {
  alert( "Hannah ist da" );
} else {
  alert( "Jemand anderes" );
}

